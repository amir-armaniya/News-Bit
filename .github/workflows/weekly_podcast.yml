name: Strategic Analysis Assistant

on:
  # Trigger 1: Runs on a schedule (every Sunday at 07:00 UTC)
  schedule:
    - cron: '0 7 * * 1'
    
  # Trigger 2: Allows manual and on-demand execution from the Cloudflare bridge
  workflow_dispatch:
    inputs:
      on_demand_input:
        description: 'The URL or command sent from the Telegram bot'
        required: false
        default: ''

jobs:
  run-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run On-Demand Analysis (if triggered by bot)
        if: github.event.inputs.on_demand_input != ''
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ON_DEMAND_INPUT: ${{ github.event.inputs.on_demand_input }}
        run: python on_demand_analyzer.py # A new script we will create

      - name: Run Scheduled Weekly Analysis (if not on-demand)
        if: github.event.inputs.on_demand_input == ''
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python main.py

      - name: Commit and push memory file
        if: github.event.inputs.on_demand_input == '' # Only commit memory on scheduled runs
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'actions-bot@github.com'
          if [ -f processed_articles.jsonl ]; then
            git add processed_articles.jsonl
            git diff --quiet && git diff --staged --quiet || (git commit -m "Update processed articles memory" && git push)
          else
            echo "No memory file to commit."
          fi
